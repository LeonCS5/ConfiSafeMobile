<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConfiSafeMobile">
        <activity
            android:name=".AccessControlActivity"
            android:exported="false" />
        <activity
            android:name=".ReportActivity"
            android:exported="false" /> <!-- Launcher atual (mantenho como você já tinha) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Telas internas -->
        <activity
            android:name=".Welcome"
            android:exported="false" />
        <activity
            android:name=".Risk_Area_Activity"
            android:exported="false" />
        <activity
            android:name=".EpiListActivity"
            android:exported="false" />
        <activity
            android:name=".EpiDanificado"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>